#!/bin/bash

while true; do
	read line
	case "$line" in
		"+")
			kill -SIGUSR1 $(cat pip6)
			;;
		"*")
			kill -SIGUSR2 $(cat pip6)
			;;
		"TERM")
			kill -SIGTERM $(cat pip6)
			exit 0
			;;
		*)
			:
			;;
	esac
done
