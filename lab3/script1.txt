#!/bin/bash

mkdir ~/test &&
{
echo "catalog test was created succesfully" >> ~/report
data=$(date +"%F_%T")
touch ~/test/"$data"
}

d=$(date +"%F_%T")
ping -c 1 www.net_nikogo.ru || echo "$d error" >> ~/report
